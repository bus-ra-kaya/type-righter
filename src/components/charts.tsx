interface TypingStats {
  wpm: number;
  accuracy: number;
  chars: number;
  topSpeed: number;
}

interface ChartProps {
  stats: TypingStats;
}

export default function Charts({ stats }: ChartProps) {


    return (
			0
		)
}
